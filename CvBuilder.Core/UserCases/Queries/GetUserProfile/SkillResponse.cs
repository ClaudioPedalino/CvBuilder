namespace CvBuilder.Core.UserCases.Queries.GetUserProfile
{
    public record SkillResponse(string Logo,
                                string Title,
                                string ShortDescription,
                                string LongDescription);
}
